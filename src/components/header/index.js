import { useState } from "react";
import { Button, Form, FormGroup, Input, Label } from "reactstrap";
import "./style.scss";

function Header() {
  return (
    <header>
      <div className="head-title">Github Job</div>
    </header>
  );
}

export default Header;
